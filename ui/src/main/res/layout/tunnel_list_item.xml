<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="org.amnezia.awg.model.ObservableTunnel" />

        <import type="org.amnezia.awg.model.ObservableTunnel.ConnectionStatus" />

        <import type="org.amnezia.awg.backend.Tunnel.State" />

        <variable
            name="collection"
            type="org.amnezia.awg.databinding.ObservableKeyedArrayList&lt;String, ObservableTunnel&gt;" />

        <variable
            name="key"
            type="String" />

        <variable
            name="item"
            type="org.amnezia.awg.model.ObservableTunnel" />

        <variable
            name="fragment"
            type="org.amnezia.awg.fragment.TunnelListFragment" />
    </data>

    <org.amnezia.awg.widget.MultiselectableRelativeLayout
        android:id="@+id/tunnel_list_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@{item.state == State.UP ? @drawable/tunnel_row_background_connected : @drawable/tunnel_row_background}"
        android:descendantFocusability="beforeDescendants"
        android:focusable="true"
        android:layout_marginHorizontal="@dimen/space_md"
        android:layout_marginVertical="@dimen/space_xs"
        android:nextFocusRight="@+id/tunnel_switch"
        android:paddingHorizontal="@dimen/space_lg"
        android:paddingVertical="@dimen/space_md">

      <LinearLayout
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:orientation="vertical">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <LinearLayout
            android:id="@+id/tunnel_row_main"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/tunnel_switch"
            android:gravity="center_vertical"
            android:orientation="horizontal">

        <ImageView
            android:id="@+id/selection_check"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="@dimen/space_md"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_check_circle"
            android:visibility="gone"
            tools:visibility="visible" />

        <LinearLayout
            android:id="@+id/tunnel_info_layout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tunnel_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@{key}"
                android:textAppearance="@style/TextAppearance.Awg.TunnelName"
                tools:text="@sample/interface_names.json/names/names/name" />

            <LinearLayout
                android:id="@+id/tunnel_status_row"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_md"
                android:layout_marginBottom="@dimen/space_xs"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <View
                    android:id="@+id/tunnel_status_dot"
                    android:layout_width="8dp"
                    android:layout_height="8dp"
                    android:layout_marginEnd="6dp"
                    android:background="@drawable/ic_status_dot"
                    app:backgroundTintColor="@{item.connectionStatus == ConnectionStatus.CONNECTED ? @color/tunnel_status_connected : item.connectionStatus == ConnectionStatus.CONNECTING ? @color/tunnel_status_connecting : @color/tunnel_status_disconnected}"
                    tools:backgroundTint="@color/tunnel_status_connected" />

                <TextView
                    android:id="@+id/tunnel_status"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{item.connectionStatus == ConnectionStatus.CONNECTED ? @string/tunnel_status_connected : item.connectionStatus == ConnectionStatus.CONNECTING ? @string/tunnel_status_connecting : @string/tunnel_status_disconnected}"
                    android:textAppearance="@style/TextAppearance.Awg.Status"
                    android:textColor="@{item.connectionStatus == ConnectionStatus.CONNECTED ? @color/tunnel_status_connected : item.connectionStatus == ConnectionStatus.CONNECTING ? @color/tunnel_status_connecting : @color/tunnel_status_disconnected}"
                    tools:text="Подключено"
                    tools:textColor="@color/tunnel_status_connected" />
            </LinearLayout>
        </LinearLayout>
        </LinearLayout>

        <org.amnezia.awg.widget.ToggleSwitch
            android:id="@+id/tunnel_switch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:nextFocusLeft="@+id/tunnel_list_item"
            app:checked="@{item.state == State.UP}"
            app:onBeforeCheckedChanged="@{fragment::setTunnelState}"
            tools:checked="@sample/interface_names.json/names/checked/checked" />
        </RelativeLayout>

        <include
            layout="@layout/include_tunnel_stats"
            app:tunnel="@{item}" />
      </LinearLayout>
    </org.amnezia.awg.widget.MultiselectableRelativeLayout>
</layout>
