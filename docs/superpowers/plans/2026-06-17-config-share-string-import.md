# Config Share-String Import (`awg://v1`) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Let users add a tunnel by pasting (or tapping) an `awg://v1` share-string or a raw `.conf`, via a refreshed add-tunnel sheet and a dedicated paste screen, with a Python CLI for producing/round-tripping the strings.

**Architecture:** A tiny crypto-free codec (`ConfigShare`) lives in the `tunnel` Android library and converts between a `.conf` text and `awg://v1/<base64url(zlib(conf))>`. The UI layer (refreshed bottom sheet + a new `PasteConfigActivity`) decodes live, previews the result, and commits it through the existing `TunnelManager.create` path. The same Activity is the target of an `awg://` deep-link intent-filter. A stdlib-only Python script (`tools/awgshare/awgshare.py`) is the reference encoder/decoder and produces the committed interop test vector.

**Tech Stack:** Java (tunnel module, `android.util` not used — pure-Java base64url), Kotlin (ui module), Android Material3, AndroidX Fragments/Activities, JUnit 4, Python 3 stdlib (`zlib`, `base64`) run via `uv`.

## Global Constraints

- **Format (verbatim from design doc):** `awg://v1/<base64url( zlib( utf8(conf_text) ) )>`. Compression = **zlib / RFC 1950** (`Deflater`/`Inflater` with `nowrap=false`). Base64URL = **RFC 4648 §5**, alphabet `-_`, **no `=` padding on encode**; decoders **must** tolerate missing padding.
- **minSdk = 24.** Do NOT use `java.util.Base64` (API 26). The codec uses a self-contained pure-Java base64url (no `android.util.Base64` dependency either, so the codec is plain-JVM unit-testable without Robolectric).
- **Charset = UTF-8**, `\n` line endings when serializing `.conf`.
- **Accepted inputs (decided):** `awg://v1/...` strings and **raw `.conf` text** only. Do **not** decode `vpn://` (AmneziaVPN Qt qCompress) or `wg://` — the add-sheet subtitle copy must say "awg:// link or .conf", not "vpn:// or wg://".
- **Deep link in scope:** register an `awg://` intent-filter → `PasteConfigActivity`.
- **Security:** the string IS the full credential. Never `Log` the string/URI; clear the input field on finish. (Design doc §Security.)
- **Name carrier convention:** a share payload MAY begin with a `# Name = <display name>` comment line. The decoder extracts it (before `Config.parse` strips comments) for the preview/tunnel name; if absent, fall back to the peer endpoint host, else `"tunnel"`.
- **Existing add-sheet `FragmentResult` contract** (`AddTunnelsSheet`): key `REQUEST_KEY_NEW_TUNNEL`, method key `REQUEST_METHOD`, values `REQUEST_CREATE`/`REQUEST_IMPORT`/`REQUEST_SCAN` — add `REQUEST_PASTE` alongside.
- Reuse existing resources: styles `TextAppearance.Awg.Title`, `TextAppearance.Awg.FieldLabel`, `TextAppearance.Awg.Mono`, `AmneziaWgTheme.Button.Save`; color `tunnel_status_connecting`; drawables `bg_icon_well`, `ic_action_scan_qr_code`, `ic_action_open`, `ic_action_edit`; dimens `space_sm/md/lg`.

---

## File Structure

**Phase 1 — Codec + CLI (pure logic, independently testable)**
- Create `tunnel/src/main/java/org/amnezia/awg/config/ConfigShare.java` — `encode`/`decode` (`awg://v1`) + base64url + zlib helpers.
- Create `tunnel/src/test/java/org/amnezia/awg/config/ConfigShareTest.java` — round-trip, padding, truncation, prefix, interop-vector tests.
- Create `tunnel/src/test/resources/share-vector.conf` — canonical fixed-key config.
- Create `tunnel/src/test/resources/share-vector.awg` — its `awg://v1` string (generated by the Python CLI).
- Create `tools/awgshare/awgshare.py` — reference encode/decode CLI (PEP 723, stdlib only).
- Create `tools/awgshare/test_awgshare.py` — pytest round-trip + interop-vector test.
- Create `tools/awgshare/README.md` — usage.

**Phase 2 — Add-tunnel sheet refresh**
- Create `ui/src/main/res/values/strings-awg-add.xml` — the add/paste strings (from refresh assets).
- Create `ui/src/main/res/drawable/ic_paste.xml` — clipboard icon (from refresh assets).
- Modify `ui/src/main/res/layout/add_tunnels_bottom_sheet.xml` — title, 4 rows (QR/file/paste/create order), amber trust warning.
- Modify `ui/src/main/java/org/amnezia/awg/fragment/AddTunnelsSheet.kt` — paste row wiring, `REQUEST_PASTE`, amber-emphasis span.
- Modify `ui/src/main/java/org/amnezia/awg/fragment/TunnelListFragment.kt` — handle `REQUEST_PASTE` → launch `PasteConfigActivity`.

**Phase 3 — Paste screen + deep link**
- Create `ui/src/main/res/layout/activity_paste_config.xml` — toolbar, helper, mono single-line field w/ paste icon, inline validation, preview card, trust warning, pinned Add button.
- Create `ui/src/main/java/org/amnezia/awg/activity/PasteConfigActivity.kt` — live decode, validation states, preview, clipboard, Add, deep-link intake.
- Create `ui/src/main/java/org/amnezia/awg/util/ShareStringDecoder.kt` — thin Kotlin wrapper: trim → `ConfigShare`/raw → `Config` + suggested name (the unit-testable decode logic).
- Create `ui/src/test/java/org/amnezia/awg/util/ShareStringDecoderTest.kt` — decode-result logic tests.
- Modify `ui/src/main/AndroidManifest.xml` — register `PasteConfigActivity` + `awg://` intent-filter.

> **Phase boundaries are natural review checkpoints.** Phase 1 ships working, tested, user-invisible software (codec + CLI). Phases 2–3 are the user-facing layer and depend on Phase 1.

---

## Task 1: `ConfigShare` codec (encode/decode)

**Files:**
- Create: `tunnel/src/main/java/org/amnezia/awg/config/ConfigShare.java`
- Test: `tunnel/src/test/java/org/amnezia/awg/config/ConfigShareTest.java`

**Interfaces:**
- Produces: `public static String ConfigShare.encode(String conf)` → `"awg://v1/..."`; `public static String ConfigShare.decode(String shareString)` → `.conf` text; throws `IllegalArgumentException` on bad prefix / corrupt payload. `public static final String ConfigShare.PREFIX = "awg://v1/"`.

- [ ] **Step 1: Write the failing test**

```java
// tunnel/src/test/java/org/amnezia/awg/config/ConfigShareTest.java
package org.amnezia.awg.config;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;
import static org.junit.Assert.fail;

import org.junit.Test;

public class ConfigShareTest {
    private static final String CONF =
        "[Interface]\n" +
        "PrivateKey = aP8A1234567890abcdefghijklmnopqrstuvwxyzABC=\n" +
        "Address = 10.8.0.2/32\n" +
        "DNS = 1.1.1.1\n" +
        "Jc = 4\nJmin = 40\nJmax = 70\n" +
        "[Peer]\n" +
        "PublicKey = bQ9B1234567890abcdefghijklmnopqrstuvwxyzABC=\n" +
        "Endpoint = 192.0.2.1:51820\n" +
        "AllowedIPs = 0.0.0.0/0\n";

    @Test public void roundTrips() {
        assertEquals(CONF, ConfigShare.decode(ConfigShare.encode(CONF)));
    }

    @Test public void encodeHasPrefix() {
        assertTrue(ConfigShare.encode(CONF).startsWith("awg://v1/"));
    }

    @Test public void encodeHasNoBase64Padding() {
        final String payload = ConfigShare.encode(CONF).substring(ConfigShare.PREFIX.length());
        assertTrue("payload must not contain '='", payload.indexOf('=') < 0);
        assertTrue("payload must be url-safe alphabet",
            payload.matches("[A-Za-z0-9_-]+"));
    }

    @Test public void decodeRejectsWrongPrefix() {
        try { ConfigShare.decode("https://example.com/x"); fail("expected IAE"); }
        catch (final IllegalArgumentException expected) { /* ok */ }
    }

    @Test public void decodeRejectsTruncatedPayload() {
        final String s = ConfigShare.encode(CONF);
        try { ConfigShare.decode(s.substring(0, s.length() - 5)); fail("expected throw"); }
        catch (final RuntimeException expected) { /* corrupt → loud failure */ }
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./gradlew :tunnel:testDebugUnitTest --tests "org.amnezia.awg.config.ConfigShareTest"`
Expected: FAIL — `ConfigShare` symbol does not exist (compilation error).

- [ ] **Step 3: Write minimal implementation**

```java
// tunnel/src/main/java/org/amnezia/awg/config/ConfigShare.java
/*
 * Copyright © 2024 AmneziaWG. All Rights Reserved.
 * SPDX-License-Identifier: Apache-2.0
 */
package org.amnezia.awg.config;

import java.io.ByteArrayOutputStream;
import java.nio.charset.StandardCharsets;
import java.util.Arrays;
import java.util.zip.DataFormatException;
import java.util.zip.Deflater;
import java.util.zip.Inflater;

/**
 * Codec for the {@code awg://v1} config share-string:
 * {@code awg://v1/<base64url( zlib( utf8(conf) ) )>}.
 * zlib = RFC 1950 (Deflater/Inflater nowrap=false); base64url = RFC 4648 §5, no padding.
 * Pure-JVM (no android.util.Base64) so it is unit-testable without Robolectric and API-24 safe.
 */
public final class ConfigShare {
    public static final String PREFIX = "awg://v1/";
    private static final char[] ENC =
        "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_".toCharArray();
    private static final int[] DEC = new int[128];
    static {
        Arrays.fill(DEC, -1);
        for (int i = 0; i < ENC.length; i++) DEC[ENC[i]] = i;
        DEC['+'] = 62; DEC['/'] = 63; // tolerate standard alphabet too
    }

    private ConfigShare() {}

    public static String encode(final String conf) {
        final byte[] raw = conf.getBytes(StandardCharsets.UTF_8);
        final Deflater deflater = new Deflater(Deflater.BEST_COMPRESSION, false); // false => zlib (RFC1950)
        deflater.setInput(raw);
        deflater.finish();
        final ByteArrayOutputStream out = new ByteArrayOutputStream();
        final byte[] tmp = new byte[1024];
        while (!deflater.finished()) out.write(tmp, 0, deflater.deflate(tmp));
        deflater.end();
        return PREFIX + b64urlEncode(out.toByteArray());
    }

    public static String decode(final String shareString) {
        if (shareString == null || !shareString.startsWith(PREFIX))
            throw new IllegalArgumentException("not an awg://v1 string");
        final byte[] comp = b64urlDecode(shareString.substring(PREFIX.length()));
        final Inflater inflater = new Inflater(false);
        inflater.setInput(comp);
        final ByteArrayOutputStream out = new ByteArrayOutputStream();
        final byte[] tmp = new byte[1024];
        try {
            while (!inflater.finished()) {
                final int n = inflater.inflate(tmp);
                if (n == 0) {
                    if (inflater.finished() || inflater.needsDictionary()) break;
                    if (inflater.needsInput()) // ran out of bytes mid-stream → truncated
                        throw new IllegalArgumentException("truncated awg://v1 payload");
                }
                out.write(tmp, 0, n);
            }
        } catch (final DataFormatException e) {
            throw new IllegalArgumentException("corrupt awg://v1 payload", e);
        } finally {
            inflater.end();
        }
        return new String(out.toByteArray(), StandardCharsets.UTF_8);
    }

    static String b64urlEncode(final byte[] data) {
        final StringBuilder sb = new StringBuilder((data.length + 2) / 3 * 4);
        int i = 0;
        while (i + 3 <= data.length) {
            final int n = ((data[i] & 0xff) << 16) | ((data[i + 1] & 0xff) << 8) | (data[i + 2] & 0xff);
            sb.append(ENC[(n >> 18) & 63]).append(ENC[(n >> 12) & 63])
              .append(ENC[(n >> 6) & 63]).append(ENC[n & 63]);
            i += 3;
        }
        final int rem = data.length - i;
        if (rem == 1) {
            final int n = (data[i] & 0xff) << 16;
            sb.append(ENC[(n >> 18) & 63]).append(ENC[(n >> 12) & 63]);
        } else if (rem == 2) {
            final int n = ((data[i] & 0xff) << 16) | ((data[i + 1] & 0xff) << 8);
            sb.append(ENC[(n >> 18) & 63]).append(ENC[(n >> 12) & 63]).append(ENC[(n >> 6) & 63]);
        }
        return sb.toString();
    }

    static byte[] b64urlDecode(final String s) {
        final int[] vals = new int[s.length()];
        int len = 0;
        for (int i = 0; i < s.length(); i++) {
            final char c = s.charAt(i);
            if (c == '=' || c == '\n' || c == '\r' || c == ' ') continue; // tolerate padding/whitespace
            final int v = c < 128 ? DEC[c] : -1;
            if (v < 0) throw new IllegalArgumentException("bad base64url character: " + c);
            vals[len++] = v;
        }
        final byte[] out = new byte[len * 6 / 8];
        int buf = 0, bits = 0, oi = 0;
        for (int i = 0; i < len; i++) {
            buf = (buf << 6) | vals[i];
            bits += 6;
            if (bits >= 8) { bits -= 8; out[oi++] = (byte) ((buf >> bits) & 0xff); }
        }
        return out;
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `./gradlew :tunnel:testDebugUnitTest --tests "org.amnezia.awg.config.ConfigShareTest"`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add tunnel/src/main/java/org/amnezia/awg/config/ConfigShare.java \
        tunnel/src/test/java/org/amnezia/awg/config/ConfigShareTest.java
git commit -m "feat(tunnel): add awg://v1 config share-string codec"
```

---

## Task 2: Python reference CLI (`awgshare.py`)

**Files:**
- Create: `tools/awgshare/awgshare.py`
- Create: `tools/awgshare/test_awgshare.py`
- Create: `tools/awgshare/README.md`

**Interfaces:**
- Produces: CLI `uv run tools/awgshare/awgshare.py {encode|decode} <file-or-arg>`; module funcs `encode(conf: str) -> str`, `decode(s: str) -> str`.

- [ ] **Step 1: Write the failing test**

```python
# tools/awgshare/test_awgshare.py
import awgshare

CONF = (
    "[Interface]\n"
    "PrivateKey = aP8A1234567890abcdefghijklmnopqrstuvwxyzABC=\n"
    "Address = 10.8.0.2/32\n"
    "[Peer]\n"
    "PublicKey = bQ9B1234567890abcdefghijklmnopqrstuvwxyzABC=\n"
    "Endpoint = 192.0.2.1:51820\n"
    "AllowedIPs = 0.0.0.0/0\n"
)

def test_round_trip():
    assert awgshare.decode(awgshare.encode(CONF)) == CONF

def test_prefix_and_no_padding():
    s = awgshare.encode(CONF)
    assert s.startswith("awg://v1/")
    assert "=" not in s[len("awg://v1/"):]

def test_decode_rejects_wrong_prefix():
    try:
        awgshare.decode("https://example.com")
        assert False, "expected ValueError"
    except ValueError:
        pass
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd tools/awgshare && uv run --with pytest pytest -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'awgshare'` (or import error).

- [ ] **Step 3: Write minimal implementation**

```python
# tools/awgshare/awgshare.py
# /// script
# requires-python = ">=3.9"
# dependencies = []
# ///
"""Reference encoder/decoder for the AmneziaWG awg://v1 config share-string.

  awg://v1/<base64url( zlib( utf8(conf) ) )>   (zlib RFC1950, base64url RFC4648 §5 no padding)

Usage:
  uv run awgshare.py encode <client.conf>     # .conf file -> awg://v1/... (prints to stdout)
  uv run awgshare.py decode <"awg://v1/...">   # share-string -> .conf  (arg or - for stdin)
"""
import argparse
import base64
import sys
import zlib

PREFIX = "awg://v1/"


def encode(conf: str) -> str:
    comp = zlib.compress(conf.encode("utf-8"), 9)  # zlib RFC1950
    return PREFIX + base64.urlsafe_b64encode(comp).rstrip(b"=").decode("ascii")


def decode(s: str) -> str:
    s = s.strip()
    if not s.startswith(PREFIX):
        raise ValueError("not an awg://v1 string")
    b = s[len(PREFIX):].encode("ascii")
    b += b"=" * (-len(b) % 4)  # re-pad
    return zlib.decompress(base64.urlsafe_b64decode(b)).decode("utf-8")


def main(argv=None) -> int:
    p = argparse.ArgumentParser(description="AmneziaWG awg://v1 share-string codec")
    sub = p.add_subparsers(dest="cmd", required=True)
    e = sub.add_parser("encode", help=".conf file -> awg://v1 string")
    e.add_argument("conf", help="path to a .conf file (or - for stdin)")
    d = sub.add_parser("decode", help="awg://v1 string -> .conf")
    d.add_argument("string", help="the awg://v1 string (or - for stdin)")
    args = p.parse_args(argv)

    if args.cmd == "encode":
        text = sys.stdin.read() if args.conf == "-" else open(args.conf, encoding="utf-8").read()
        sys.stdout.write(encode(text) + "\n")
    elif args.cmd == "decode":
        text = sys.stdin.read() if args.string == "-" else args.string
        sys.stdout.write(decode(text))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd tools/awgshare && uv run --with pytest pytest -q`
Expected: PASS (3 tests).

- [ ] **Step 5: Write the README**

```markdown
# awgshare — AmneziaWG awg://v1 share-string tool

Reference encoder/decoder for the config share-string the Android app imports.

    awg://v1/<base64url( zlib( utf8(conf) ) )>

## Use

    uv run awgshare.py encode client.conf      # -> awg://v1/... (paste into the app)
    uv run awgshare.py decode "awg://v1/..."    # -> the original .conf
    cat client.conf | uv run awgshare.py encode -

Stdlib only (`zlib`, `base64`); `uv` just provides the runner. Plain `python3 awgshare.py ...` also works.

## Optional tunnel name

Prefix the .conf with a comment to carry a display name into the app's preview:

    # Name = awg-fi-01
    [Interface]
    ...

## Test

    uv run --with pytest pytest -q
```

- [ ] **Step 6: Commit**

```bash
git add tools/awgshare/awgshare.py tools/awgshare/test_awgshare.py tools/awgshare/README.md
git commit -m "feat(tools): add awgshare.py reference CLI for awg://v1 strings"
```

---

## Task 3: Cross-language interop test vector

Pins that a string produced by the Python tool decodes byte-for-byte in the Java codec. (Per design doc: encode-byte-equality across zlib builds is NOT the contract; `decode(fixture) == conf` and cross round-trip IS.)

**Files:**
- Create: `tunnel/src/test/resources/share-vector.conf`
- Create: `tunnel/src/test/resources/share-vector.awg`
- Modify: `tunnel/src/test/java/org/amnezia/awg/config/ConfigShareTest.java`

**Interfaces:**
- Consumes: `ConfigShare.decode` (Task 1), `awgshare.encode` (Task 2).

- [ ] **Step 1: Create the canonical fixture config**

```ini
# tunnel/src/test/resources/share-vector.conf
# Name = awg-fi-01
[Interface]
PrivateKey = aP8A1234567890abcdefghijklmnopqrstuvwxyzABC=
Address = 10.8.0.2/32
DNS = 1.1.1.1
Jc = 4
Jmin = 40
Jmax = 70
[Peer]
PublicKey = bQ9B1234567890abcdefghijklmnopqrstuvwxyzABC=
Endpoint = 192.0.2.1:51820
AllowedIPs = 0.0.0.0/0
```

- [ ] **Step 2: Generate the vector string with the Python tool**

Run:
```bash
uv run tools/awgshare/awgshare.py encode tunnel/src/test/resources/share-vector.conf \
  | tr -d '\n' > tunnel/src/test/resources/share-vector.awg
```
Expected: `tunnel/src/test/resources/share-vector.awg` contains a single `awg://v1/...` line, no trailing newline.

- [ ] **Step 3: Write the failing interop test (append to `ConfigShareTest.java`)**

```java
    @Test public void decodesPythonGeneratedVector() throws Exception {
        final String conf = readResource("share-vector.conf");
        final String shareString = readResource("share-vector.awg").trim();
        assertEquals(conf, ConfigShare.decode(shareString));
    }

    @Test public void javaEncodeRoundTripsVectorConf() throws Exception {
        final String conf = readResource("share-vector.conf");
        assertEquals(conf, ConfigShare.decode(ConfigShare.encode(conf)));
    }

    private static String readResource(final String name) throws java.io.IOException {
        try (final java.io.InputStream in = ConfigShareTest.class.getClassLoader().getResourceAsStream(name)) {
            if (in == null) throw new java.io.IOException("missing resource " + name);
            final java.io.ByteArrayOutputStream bos = new java.io.ByteArrayOutputStream();
            final byte[] buf = new byte[1024];
            int n;
            while ((n = in.read(buf)) != -1) bos.write(buf, 0, n);
            return new String(bos.toByteArray(), java.nio.charset.StandardCharsets.UTF_8);
        }
    }
```

> Note: `share-vector.conf` must be committed with `\n` line endings (the encoder hashed exactly these bytes). Configure `.gitattributes` if the repo is checked out on Windows: `*.conf text eol=lf`.

- [ ] **Step 4: Run test to verify it passes**

Run: `./gradlew :tunnel:testDebugUnitTest --tests "org.amnezia.awg.config.ConfigShareTest"`
Expected: PASS (7 tests).

- [ ] **Step 5: Add the same vector to the Python test**

Append to `tools/awgshare/test_awgshare.py`:
```python
import os

def test_decodes_committed_vector():
    here = os.path.dirname(__file__)
    root = os.path.abspath(os.path.join(here, "..", ".."))
    conf = open(os.path.join(root, "tunnel/src/test/resources/share-vector.conf"), encoding="utf-8").read()
    s = open(os.path.join(root, "tunnel/src/test/resources/share-vector.awg"), encoding="utf-8").read().strip()
    assert awgshare.decode(s) == conf
```

- [ ] **Step 6: Run and commit**

Run: `cd tools/awgshare && uv run --with pytest pytest -q`  → PASS (4 tests)
```bash
git add tunnel/src/test/resources/share-vector.conf tunnel/src/test/resources/share-vector.awg \
        tunnel/src/test/java/org/amnezia/awg/config/ConfigShareTest.java \
        tools/awgshare/test_awgshare.py
git commit -m "test: pin awg://v1 cross-language interop vector"
```

---

## Task 4: `ShareStringDecoder` (Kotlin decode-to-Config wrapper)

The unit-testable bridge from raw input → a `Config` + suggested name. Handles `awg://v1`, raw `.conf`, trimming, and the `# Name =` convention.

**Files:**
- Create: `ui/src/main/java/org/amnezia/awg/util/ShareStringDecoder.kt`
- Test: `ui/src/test/java/org/amnezia/awg/util/ShareStringDecoderTest.kt`

**Interfaces:**
- Consumes: `ConfigShare.decode` (Task 1); `org.amnezia.awg.config.Config.parse(InputStream)`.
- Produces:
  - `data class DecodedShare(val configText: String, val config: Config, val name: String, val endpoint: String?, val obfuscation: String)`
  - `object ShareStringDecoder { fun decode(input: String): DecodedShare }` — throws on invalid (callers catch to show `paste_invalid`).

> `ui/src/test` may not exist yet. Confirm `ui/build.gradle.kts` has `testImplementation(libs.junit)`; if absent add it under `dependencies`. The `Config`/`ConfigShare` types come from the `tunnel` module (already an `implementation` dependency of `ui`), so they are on the unit-test classpath.

- [ ] **Step 1: Write the failing test**

```kotlin
// ui/src/test/java/org/amnezia/awg/util/ShareStringDecoderTest.kt
package org.amnezia.awg.util

import org.amnezia.awg.config.ConfigShare
import org.junit.Assert.assertEquals
import org.junit.Assert.assertTrue
import org.junit.Test

class ShareStringDecoderTest {
    private val conf = """
        # Name = awg-fi-01
        [Interface]
        PrivateKey = aP8A1234567890abcdefghijklmnopqrstuvwxyzABC=
        Address = 10.8.0.2/32
        [Peer]
        PublicKey = bQ9B1234567890abcdefghijklmnopqrstuvwxyzABC=
        Endpoint = 192.0.2.1:51820
        AllowedIPs = 0.0.0.0/0
    """.trimIndent() + "\n"

    @Test fun decodesAwgString() {
        val r = ShareStringDecoder.decode("  " + ConfigShare.encode(conf) + "  ")
        assertEquals("awg-fi-01", r.name)
        assertEquals("192.0.2.1:51820", r.endpoint)
    }

    @Test fun decodesRawConf() {
        val r = ShareStringDecoder.decode(conf)
        assertEquals("awg-fi-01", r.name)
    }

    @Test fun fallsBackToEndpointHostWhenNoNameComment() {
        val noName = conf.replace("# Name = awg-fi-01\n", "")
        val r = ShareStringDecoder.decode(noName)
        assertEquals("192.0.2.1", r.name)
    }

    @Test(expected = IllegalArgumentException::class)
    fun rejectsGarbage() {
        ShareStringDecoder.decode("not a config at all")
    }

    @Test fun obfuscationSummaryReflectsPresence() {
        val r = ShareStringDecoder.decode(conf)
        assertTrue(r.obfuscation.isNotEmpty())
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./gradlew :ui:testDebugUnitTest --tests "org.amnezia.awg.util.ShareStringDecoderTest"`
Expected: FAIL — `ShareStringDecoder` unresolved.

- [ ] **Step 3: Write minimal implementation**

```kotlin
// ui/src/main/java/org/amnezia/awg/util/ShareStringDecoder.kt
package org.amnezia.awg.util

import org.amnezia.awg.config.Config
import org.amnezia.awg.config.ConfigShare
import java.io.ByteArrayInputStream
import java.nio.charset.StandardCharsets

data class DecodedShare(
    val configText: String,
    val config: Config,
    val name: String,
    val endpoint: String?,
    val obfuscation: String,
)

object ShareStringDecoder {
    private val NAME_COMMENT = Regex("""(?im)^\s*#\s*Name\s*=\s*(.+?)\s*$""")

    fun decode(input: String): DecodedShare {
        val trimmed = input.trim()
        require(trimmed.isNotEmpty()) { "empty input" }
        val confText = if (trimmed.startsWith(ConfigShare.PREFIX)) {
            ConfigShare.decode(trimmed)            // throws IllegalArgumentException on corruption
        } else {
            trimmed                                 // assume raw .conf
        }
        val config = Config.parse(ByteArrayInputStream(confText.toByteArray(StandardCharsets.UTF_8)))
        val endpoint = config.peers.firstOrNull()?.endpoint?.orElse(null)?.toString()
        val name = NAME_COMMENT.find(confText)?.groupValues?.get(1)?.takeIf { it.isNotBlank() }
            ?: endpoint?.substringBefore(':')?.takeIf { it.isNotBlank() }
            ?: "tunnel"
        return DecodedShare(confText, config, name, endpoint, obfuscationSummary(config))
    }

    private fun obfuscationSummary(config: Config): String {
        // Compact human summary for the preview card; tolerant of the Interface API surface.
        val i = config.`interface`
        val parts = mutableListOf<String>()
        runCatching { if (i.junkPacketCount.isPresent) parts += "Jc ${i.junkPacketCount.asInt}" }
        runCatching { if (i.initPacketMagicHeader.isPresent) parts += "H1–H4" }
        return if (parts.isEmpty()) "None" else parts.joinToString(" · ")
    }
}
```

> **Verify against the real `Interface`/`Peer` API before running.** The accessor names above (`peers`, `endpoint`, `junkPacketCount`, `initPacketMagicHeader`, `Optional`-returning getters) must match `tunnel/src/main/java/org/amnezia/awg/config/Interface.java` and `Peer.java`. Open those files, adjust the getter names/types, and keep `obfuscationSummary` defensive (wrap optional reads in `runCatching`). The summary string is cosmetic — correctness of `name`/`endpoint` is what the tests assert.

- [ ] **Step 4: Run test to verify it passes**

Run: `./gradlew :ui:testDebugUnitTest --tests "org.amnezia.awg.util.ShareStringDecoderTest"`
Expected: PASS (5 tests). If `obfuscationSummaryReflectsPresence` fails because the fixture has no obfuscation getter match, adjust `obfuscationSummary` to return `"None"` and keep the test asserting `isNotEmpty()` (already true for `"None"`).

- [ ] **Step 5: Commit**

```bash
git add ui/src/main/java/org/amnezia/awg/util/ShareStringDecoder.kt \
        ui/src/test/java/org/amnezia/awg/util/ShareStringDecoderTest.kt
git commit -m "feat(ui): add ShareStringDecoder (awg://v1 + raw .conf -> Config)"
```

---

## Task 5: Add/paste strings + paste icon

**Files:**
- Create: `ui/src/main/res/values/strings-awg-add.xml`
- Create: `ui/src/main/res/drawable/ic_paste.xml`

> Source of truth: `~/Documents/amneziawg-refresh-assets/res/values/strings-awg-add.xml` and `.../res/drawable/ic_paste.xml`. **Correct the subtitle copy** per the Global Constraints (no `vpn://`/`wg://`).

- [ ] **Step 1: Create the strings file**

```xml
<?xml version="1.0" encoding="utf-8"?>
<!-- Add-tunnel sheet + paste-string screen. -->
<resources>
    <string name="add_tunnel_title">Add a tunnel</string>
    <string name="add_paste_string">Paste config string</string>
    <string name="add_paste_string_sub">awg:// link or .conf text</string>

    <!-- Security warning (sheet AND paste screen) -->
    <string name="add_trust_warning">Only add configs from sources you trust — amnezia.org or people you know personally.</string>

    <!-- Paste-string screen -->
    <string name="paste_title">Paste config string</string>
    <string name="paste_hint">Paste an encoded config link to decode it into a tunnel.</string>
    <string name="paste_field_label">Config string</string>
    <string name="paste_from_clipboard">Paste from clipboard</string>
    <string name="paste_valid">Valid AmneziaWG link — “%1$s”</string>
    <string name="paste_invalid">Couldn’t decode this string. Check you copied the whole link.</string>
    <string name="paste_preview">Decoded preview</string>
    <string name="paste_preview_name">Name</string>
    <string name="paste_preview_endpoint">Endpoint</string>
    <string name="paste_preview_obfuscation">Obfuscation</string>
    <string name="paste_add">Add tunnel</string>
</resources>
```

> The sheet reuses the app's existing `create_from_file`/`add_subtitle_file`, `create_from_qr_code`/`add_subtitle_qr`, `create_empty`/`add_subtitle_empty` strings (already in `ui/src/main/res/values/strings.xml`) — only the title, paste row, and trust warning are new. `add_tunnel_title` is added here because the current sheet has no title string.

- [ ] **Step 2: Create the paste icon**

```xml
<?xml version="1.0" encoding="utf-8"?>
<!-- Paste config string: clipboard with a line of text. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp" android:height="24dp"
    android:viewportWidth="24" android:viewportHeight="24"
    android:tint="?attr/colorPrimary">
    <path android:fillColor="#00000000" android:strokeColor="@android:color/white"
        android:strokeWidth="2" android:strokeLineCap="round" android:strokeLineJoin="round"
        android:pathData="M4,7 V5 a1,1 0 0 1 1,-1 h14 a1,1 0 0 1 1,1 v2 M9,20 h6 M12,4 v16 M7,11 h4 M7,15 h2" />
</vector>
```

- [ ] **Step 3: Verify resources compile**

Run: `./gradlew :ui:processDebugResources`
Expected: BUILD SUCCESSFUL (no duplicate-resource errors).

- [ ] **Step 4: Commit**

```bash
git add ui/src/main/res/values/strings-awg-add.xml ui/src/main/res/drawable/ic_paste.xml
git commit -m "feat(ui): add paste-config strings and ic_paste icon"
```

---

## Task 6: Refresh the add-tunnel sheet (title, 4 rows, trust warning)

**Files:**
- Modify: `ui/src/main/res/layout/add_tunnels_bottom_sheet.xml`
- Modify: `ui/src/main/java/org/amnezia/awg/fragment/AddTunnelsSheet.kt`

**Interfaces:**
- Produces: new constant `AddTunnelsSheet.REQUEST_PASTE = "request_paste"`; new view id `@+id/create_from_paste`; the sheet emits `REQUEST_PASTE` via the existing `setFragmentResult(REQUEST_KEY_NEW_TUNNEL, …)` contract.

- [ ] **Step 1: Rewrite the sheet layout**

Replace the contents of `ui/src/main/res/layout/add_tunnels_bottom_sheet.xml` with the following. Order per SPEC-add-tunnel §A: title → QR → file → **paste** → create → amber trust warning. (Keeps `bg_icon_well`, `space_*` dims, existing icons; adds the paste row + replaces the link disclaimer with the amber trust warning.)

```xml
<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/space_sm">

    <com.google.android.material.bottomsheet.BottomSheetDragHandleView
        android:id="@+id/drag_handle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/sheet_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_sm"
        android:layout_marginBottom="@dimen/space_sm"
        android:text="@string/add_tunnel_title"
        android:textAppearance="@style/TextAppearance.Awg.Title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/drag_handle" />

    <!-- 1 · Scan QR -->
    <LinearLayout
        android:id="@+id/create_from_qrcode"
        style="@style/AddTunnelRow"
        android:nextFocusDown="@id/create_from_file"
        app:layout_constraintTop_toBottomOf="@id/sheet_title">
        <FrameLayout style="@style/AddTunnelIconWell">
            <ImageView style="@style/AddTunnelIcon" android:src="@drawable/ic_action_scan_qr_code" />
        </FrameLayout>
        <LinearLayout style="@style/AddTunnelText">
            <TextView style="@style/AddTunnelTitle" android:text="@string/create_from_qr_code" />
            <TextView style="@style/AddTunnelSubtitle" android:text="@string/add_subtitle_qr" />
        </LinearLayout>
    </LinearLayout>

    <!-- 2 · Import from file -->
    <LinearLayout
        android:id="@+id/create_from_file"
        style="@style/AddTunnelRow"
        android:nextFocusUp="@id/create_from_qrcode"
        android:nextFocusDown="@id/create_from_paste"
        app:layout_constraintTop_toBottomOf="@id/create_from_qrcode">
        <FrameLayout style="@style/AddTunnelIconWell">
            <ImageView style="@style/AddTunnelIcon" android:src="@drawable/ic_action_open" />
        </FrameLayout>
        <LinearLayout style="@style/AddTunnelText">
            <TextView style="@style/AddTunnelTitle" android:text="@string/create_from_file" />
            <TextView style="@style/AddTunnelSubtitle" android:text="@string/add_subtitle_file" />
        </LinearLayout>
    </LinearLayout>

    <!-- 3 · Paste config string (NEW) -->
    <LinearLayout
        android:id="@+id/create_from_paste"
        style="@style/AddTunnelRow"
        android:nextFocusUp="@id/create_from_file"
        android:nextFocusDown="@id/create_empty"
        app:layout_constraintTop_toBottomOf="@id/create_from_file">
        <FrameLayout style="@style/AddTunnelIconWell">
            <ImageView style="@style/AddTunnelIcon" android:src="@drawable/ic_paste" />
        </FrameLayout>
        <LinearLayout style="@style/AddTunnelText">
            <TextView style="@style/AddTunnelTitle" android:text="@string/add_paste_string" />
            <TextView style="@style/AddTunnelSubtitle" android:text="@string/add_paste_string_sub" />
        </LinearLayout>
    </LinearLayout>

    <!-- 4 · Create from scratch -->
    <LinearLayout
        android:id="@+id/create_empty"
        style="@style/AddTunnelRow"
        android:nextFocusUp="@id/create_from_paste"
        android:nextFocusDown="@id/trust_warning"
        app:layout_constraintTop_toBottomOf="@id/create_from_paste">
        <FrameLayout style="@style/AddTunnelIconWell">
            <ImageView style="@style/AddTunnelIcon" android:src="@drawable/ic_action_edit" />
        </FrameLayout>
        <LinearLayout style="@style/AddTunnelText">
            <TextView style="@style/AddTunnelTitle" android:text="@string/create_empty" />
            <TextView style="@style/AddTunnelSubtitle" android:text="@string/add_subtitle_empty" />
        </LinearLayout>
    </LinearLayout>

    <!-- Trust warning: amber triangle + caption, amnezia.org emphasized in code -->
    <LinearLayout
        android:id="@+id/trust_warning"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/space_lg"
        android:layout_marginTop="@dimen/space_md"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/create_empty">
        <ImageView
            android:layout_width="15dp"
            android:layout_height="15dp"
            android:layout_marginEnd="@dimen/space_sm"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_warning_triangle"
            app:tint="@color/tunnel_status_connecting" />
        <TextView
            android:id="@+id/trust_warning_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/add_trust_warning"
            android:textAppearance="@style/TextAppearance.Awg.FieldLabel"
            android:textColor="?attr/colorOnSurfaceVariant" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
```

- [ ] **Step 2: Add the shared row styles (so the layout above resolves)**

Append to `ui/src/main/res/values/styles-awg-type.xml` (the file that already defines `TextAppearance.Awg.*`):

```xml
    <style name="AddTunnelRow">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingHorizontal">@dimen/space_lg</item>
        <item name="android:paddingVertical">@dimen/space_md</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
    </style>
    <style name="AddTunnelIconWell">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:background">@drawable/bg_icon_well</item>
    </style>
    <style name="AddTunnelIcon">
        <item name="android:layout_width">20dp</item>
        <item name="android:layout_height">20dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:importantForAccessibility">no</item>
        <item name="android:tint">?attr/colorOnPrimaryContainer</item>
    </style>
    <style name="AddTunnelText">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">@dimen/space_lg</item>
        <item name="android:layout_weight">1</item>
        <item name="android:orientation">vertical</item>
    </style>
    <style name="AddTunnelTitle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAppearance">?attr/textAppearanceTitleMedium</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
    </style>
    <style name="AddTunnelSubtitle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAppearance">?attr/textAppearanceBodySmall</item>
        <item name="android:textColor">?attr/colorOnSurfaceVariant</item>
    </style>
```

> Confirm `ui/src/main/res/values/styles-awg-type.xml` is where `TextAppearance.Awg.Title` lives (it is, per the resource scan). If the project prefers `<item name="app:layout_constraint…">` constraint attrs are not valid inside `<style>` for ConstraintLayout in older AGP — they ARE valid as `layout_constraintStart_toStartOf` style items here because they are layout params; if the build rejects them, inline `app:layout_constraintStart_toStartOf="parent"` / `…End…` on each row instead and drop those two style items.

- [ ] **Step 3: Create the warning-triangle drawable**

```xml
<?xml version="1.0" encoding="utf-8"?>
<!-- ui/src/main/res/drawable/ic_warning_triangle.xml -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp" android:height="24dp"
    android:viewportWidth="24" android:viewportHeight="24">
    <path android:fillColor="#00000000" android:strokeColor="@android:color/white"
        android:strokeWidth="2" android:strokeLineCap="round" android:strokeLineJoin="round"
        android:pathData="M10.29,3.86 L1.82,18 a2,2 0 0 0 1.71,3 h16.94 a2,2 0 0 0 1.71,-3 L13.71,3.86 a2,2 0 0 0 -3.42,0 z M12,9 v4 M12,17 h0.01" />
</vector>
```

- [ ] **Step 4: Wire the paste row + amber emphasis in `AddTunnelsSheet.kt`**

In `onViewCreated`'s global-layout listener, add a click handler beside the existing three:
```kotlin
                dialog.findViewById<View>(R.id.create_from_paste)?.setOnClickListener {
                    dismiss()
                    onRequestPasteString()
                }
```
Add the emit method beside `onRequestScanQRCode`:
```kotlin
    private fun onRequestPasteString() {
        setFragmentResult(REQUEST_KEY_NEW_TUNNEL, bundleOf(REQUEST_METHOD to REQUEST_PASTE))
    }
```
Add the constant in the `companion object`:
```kotlin
        const val REQUEST_PASTE = "request_paste"
```
Emphasize "amnezia.org" in the trust caption — in `onCreateView`, replace the old `disclaimer` `movementMethod` line with:
```kotlin
        view.findViewById<TextView>(R.id.trust_warning_text)?.let { tv ->
            val full = getString(R.string.add_trust_warning)
            val mark = "amnezia.org"
            val i = full.indexOf(mark)
            if (i >= 0) {
                val sp = android.text.SpannableString(full)
                val onSurface = com.google.android.material.color.MaterialColors.getColor(tv, com.google.android.material.R.attr.colorOnSurface)
                sp.setSpan(android.text.style.ForegroundColorSpan(onSurface), i, i + mark.length, android.text.Spannable.SPAN_EXCLUSIVE_EXCLUSIVE)
                sp.setSpan(android.text.style.StyleSpan(android.graphics.Typeface.BOLD), i, i + mark.length, android.text.Spannable.SPAN_EXCLUSIVE_EXCLUSIVE)
                tv.text = sp
            }
        }
```
Remove the now-deleted `R.id.disclaimer` reference (the old `LinkMovementMethod` block) since the layout no longer has it.

- [ ] **Step 5: Build to verify it compiles**

Run: `./gradlew :ui:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 6: Manual smoke test**

Launch the app, tap the add-tunnel FAB. Verify: title "Add a tunnel"; four rows in order QR / file / **Paste config string** (clipboard icon, subtitle "awg:// link or .conf text") / create; amber warning triangle + caption with **amnezia.org** emphasized. Tapping "Paste config string" currently does nothing yet (wired in Task 8) — that's expected.

- [ ] **Step 7: Commit**

```bash
git add ui/src/main/res/layout/add_tunnels_bottom_sheet.xml \
        ui/src/main/res/values/styles-awg-type.xml \
        ui/src/main/res/drawable/ic_warning_triangle.xml \
        ui/src/main/java/org/amnezia/awg/fragment/AddTunnelsSheet.kt
git commit -m "feat(ui): refresh add-tunnel sheet — title, 4 rows, paste, trust warning"
```

---

## Task 7: Paste-config screen layout

**Files:**
- Create: `ui/src/main/res/layout/activity_paste_config.xml`

> Single-line mono field (NOT a textarea) modeled on the editor's `TextInputLayout` usage in `tunnel_editor_fragment.xml`; preview card hidden until valid; "Add tunnel" pinned bottom, disabled by default.

- [ ] **Step 1: Create the layout**

```xml
<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:title="@string/paste_title" />
    </com.google.android.material.appbar.AppBarLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/space_lg">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/space_md"
                android:text="@string/paste_hint"
                android:textAppearance="@style/TextAppearance.Awg.FieldLabel"
                android:textColor="?attr/colorOnSurfaceVariant" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/paste_field_label"
                android:textAllCaps="true"
                android:textAppearance="@style/TextAppearance.Awg.FieldLabel"
                android:textColor="?attr/colorPrimary" />

            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/string_input_layout"
                style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_sm"
                app:boxCornerRadiusBottomEnd="12dp"
                app:boxCornerRadiusBottomStart="12dp"
                app:boxCornerRadiusTopEnd="12dp"
                app:boxCornerRadiusTopStart="12dp"
                app:endIconDrawable="@drawable/ic_paste"
                app:endIconMode="custom"
                app:endIconContentDescription="@string/paste_from_clipboard">
                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/string_input"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:fontFamily="@font/jetbrains_mono_family"
                    android:inputType="textUri"
                    android:maxLines="1"
                    android:singleLine="true"
                    android:textAppearance="@style/TextAppearance.Awg.Mono" />
            </com.google.android.material.textfield.TextInputLayout>

            <TextView
                android:id="@+id/validation_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_sm"
                android:drawablePadding="@dimen/space_sm"
                android:gravity="center_vertical"
                android:textAppearance="@style/TextAppearance.Awg.FieldLabel"
                android:visibility="gone"
                tools:ignore="MissingPrefix" />

            <!-- Decoded preview (visible only when valid) -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/preview_card"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_lg"
                android:visibility="gone"
                app:cardBackgroundColor="?attr/colorSurfaceContainerHigh"
                app:cardCornerRadius="13dp"
                app:cardElevation="0dp">
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="@dimen/space_lg">
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginBottom="@dimen/space_md"
                        android:text="@string/paste_preview"
                        android:textAllCaps="true"
                        android:textAppearance="@style/TextAppearance.Awg.FieldLabel"
                        android:textColor="?attr/colorPrimary" />
                    <TextView android:id="@+id/preview_name_label" style="@style/TextAppearance.Awg.FieldLabel" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="@string/paste_preview_name" android:textColor="?attr/colorOnSurfaceVariant" />
                    <TextView android:id="@+id/preview_name" style="@style/TextAppearance.Awg.FieldValue" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginBottom="@dimen/space_sm" />
                    <TextView android:id="@+id/preview_endpoint_label" style="@style/TextAppearance.Awg.FieldLabel" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="@string/paste_preview_endpoint" android:textColor="?attr/colorOnSurfaceVariant" />
                    <TextView android:id="@+id/preview_endpoint" style="@style/TextAppearance.Awg.Mono" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginBottom="@dimen/space_sm" />
                    <TextView android:id="@+id/preview_obfuscation_label" style="@style/TextAppearance.Awg.FieldLabel" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="@string/paste_preview_obfuscation" android:textColor="?attr/colorOnSurfaceVariant" />
                    <TextView android:id="@+id/preview_obfuscation" style="@style/TextAppearance.Awg.FieldValue" android:layout_width="wrap_content" android:layout_height="wrap_content" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <!-- Trust warning (repeat) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_lg"
                android:gravity="center_vertical"
                android:orientation="horizontal">
                <ImageView
                    android:layout_width="15dp"
                    android:layout_height="15dp"
                    android:layout_marginEnd="@dimen/space_sm"
                    android:importantForAccessibility="no"
                    android:src="@drawable/ic_warning_triangle"
                    app:tint="@color/tunnel_status_connecting" />
                <TextView
                    android:id="@+id/trust_warning_text"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/add_trust_warning"
                    android:textAppearance="@style/TextAppearance.Awg.FieldLabel"
                    android:textColor="?attr/colorOnSurfaceVariant" />
            </LinearLayout>
        </LinearLayout>
    </ScrollView>

    <!-- Pinned Add button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/add_button"
        style="@style/AmneziaWgTheme.Button.Save"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_margin="@dimen/space_lg"
        android:enabled="false"
        android:text="@string/paste_add" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
```

> Two resource checks before building: (a) confirm `ic_arrow_back` exists in `ui` drawables (the toolbar back icon) — if the project uses a different name (e.g. `ic_action_back`), use that; (b) confirm `colorSurfaceContainerHigh` attr resolves in the app theme (it should under Material3). Add `xmlns:tools` to the root if you keep the `tools:ignore`.

- [ ] **Step 2: Verify resources compile**

Run: `./gradlew :ui:processDebugResources`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: Commit**

```bash
git add ui/src/main/res/layout/activity_paste_config.xml
git commit -m "feat(ui): add paste-config screen layout"
```

---

## Task 8: `PasteConfigActivity` (live decode, preview, add, deep link)

**Files:**
- Create: `ui/src/main/java/org/amnezia/awg/activity/PasteConfigActivity.kt`

**Interfaces:**
- Consumes: `ShareStringDecoder.decode` (Task 4), `DecodedShare` (Task 4), `Application.getTunnelManager().create(name, config)` (existing — `suspend`, used in `TunnelImporter`), layout/ids from Task 7.
- Produces: an exported entry point that imports from (a) the sheet (plain launch) and (b) an `awg://` deep link (`intent.data`).

- [ ] **Step 1: Implement the activity**

```kotlin
// ui/src/main/java/org/amnezia/awg/activity/PasteConfigActivity.kt
package org.amnezia.awg.activity

import android.content.ClipboardManager
import android.content.Context
import android.graphics.Typeface
import android.os.Bundle
import android.text.Editable
import android.text.Spannable
import android.text.SpannableString
import android.text.TextWatcher
import android.text.style.ForegroundColorSpan
import android.text.style.StyleSpan
import android.view.View
import android.widget.TextView
import androidx.appcompat.app.AppCompatActivity
import androidx.core.widget.doAfterTextChanged
import androidx.lifecycle.lifecycleScope
import com.google.android.material.button.MaterialButton
import com.google.android.material.card.MaterialCardView
import com.google.android.material.color.MaterialColors
import com.google.android.material.textfield.TextInputEditText
import com.google.android.material.textfield.TextInputLayout
import com.google.android.material.appbar.MaterialToolbar
import kotlinx.coroutines.launch
import org.amnezia.awg.Application
import org.amnezia.awg.R
import org.amnezia.awg.util.DecodedShare
import org.amnezia.awg.util.ShareStringDecoder

class PasteConfigActivity : AppCompatActivity() {
    private var decoded: DecodedShare? = null

    private lateinit var input: TextInputEditText
    private lateinit var inputLayout: TextInputLayout
    private lateinit var validation: TextView
    private lateinit var previewCard: MaterialCardView
    private lateinit var addButton: MaterialButton

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_paste_config)

        findViewById<MaterialToolbar>(R.id.toolbar).setNavigationOnClickListener { finish() }
        input = findViewById(R.id.string_input)
        inputLayout = findViewById(R.id.string_input_layout)
        validation = findViewById(R.id.validation_text)
        previewCard = findViewById(R.id.preview_card)
        addButton = findViewById(R.id.add_button)
        emphasizeAmnezia(findViewById(R.id.trust_warning_text))

        input.doAfterTextChanged { validate(it?.toString().orEmpty()) }
        inputLayout.setEndIconOnClickListener { pasteFromClipboard() }
        addButton.setOnClickListener { commit() }

        // Entry (a): deep link awg://...  Entry (b): clipboard auto-fill if it already holds one.
        val deepLink = intent?.data?.toString()
        when {
            deepLink != null && deepLink.startsWith("awg://") -> input.setText(deepLink)
            else -> clipboardText()?.takeIf { it.trim().startsWith("awg://") }?.let { input.setText(it.trim()) }
        }
    }

    private fun validate(raw: String) {
        if (raw.isBlank()) { setNeutral(); return }
        try {
            val d = ShareStringDecoder.decode(raw)
            decoded = d
            setValid(d)
        } catch (e: Throwable) {
            decoded = null
            setInvalid()
        }
    }

    private fun setNeutral() {
        decoded = null
        validation.visibility = View.GONE
        previewCard.visibility = View.GONE
        addButton.isEnabled = false
    }

    private fun setInvalid() {
        validation.visibility = View.VISIBLE
        validation.text = getString(R.string.paste_invalid)
        val err = MaterialColors.getColor(validation, com.google.android.material.R.attr.colorError)
        validation.setTextColor(err)
        validation.setCompoundDrawablesRelativeWithIntrinsicBounds(R.drawable.ic_warning_triangle, 0, 0, 0)
        previewCard.visibility = View.GONE
        addButton.isEnabled = false
    }

    private fun setValid(d: DecodedShare) {
        validation.visibility = View.VISIBLE
        validation.text = getString(R.string.paste_valid, d.name)
        val ok = MaterialColors.getColor(validation, com.google.android.material.R.attr.colorPrimary)
        validation.setTextColor(ok)
        validation.setCompoundDrawablesRelativeWithIntrinsicBounds(R.drawable.ic_check, 0, 0, 0)
        findViewById<TextView>(R.id.preview_name).text = d.name
        findViewById<TextView>(R.id.preview_endpoint).text = d.endpoint ?: "—"
        findViewById<TextView>(R.id.preview_obfuscation).text = d.obfuscation
        previewCard.visibility = View.VISIBLE
        addButton.isEnabled = true
    }

    private fun commit() {
        val d = decoded ?: return
        addButton.isEnabled = false
        lifecycleScope.launch {
            try {
                Application.getTunnelManager().create(d.name, d.config)
                input.text?.clear()                       // security: clear the credential field
                finish()
            } catch (e: Throwable) {
                addButton.isEnabled = true
                setInvalid()                              // surface failure rather than crash
            }
        }
    }

    private fun pasteFromClipboard() { clipboardText()?.let { input.setText(it.trim()) } }

    private fun clipboardText(): String? {
        val cm = getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager
        return cm.primaryClip?.takeIf { it.itemCount > 0 }?.getItemAt(0)?.coerceToText(this)?.toString()
    }

    private fun emphasizeAmnezia(tv: TextView) {
        val full = getString(R.string.add_trust_warning)
        val mark = "amnezia.org"
        val i = full.indexOf(mark)
        if (i < 0) return
        val sp = SpannableString(full)
        val onSurface = MaterialColors.getColor(tv, com.google.android.material.R.attr.colorOnSurface)
        sp.setSpan(ForegroundColorSpan(onSurface), i, i + mark.length, Spannable.SPAN_EXCLUSIVE_EXCLUSIVE)
        sp.setSpan(StyleSpan(Typeface.BOLD), i, i + mark.length, Spannable.SPAN_EXCLUSIVE_EXCLUSIVE)
        tv.text = sp
    }
}
```

> **Two resource checks:** (a) `R.drawable.ic_check` for the valid state — confirm a check drawable exists (the app has `ic_check_box_checked`; if no plain check, create `ic_check.xml` with a checkmark path or reuse an existing tick). (b) Confirm `TunnelManager.create(name: String, config: Config)` is `suspend` and returns the tunnel (it is, per `TunnelImporter` line 84/89). Do **not** add any `Log.d(... shareString ...)` — Global Constraints forbid logging the credential.

- [ ] **Step 2: Build to verify it compiles**

Run: `./gradlew :ui:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: Commit**

```bash
git add ui/src/main/java/org/amnezia/awg/activity/PasteConfigActivity.kt
git commit -m "feat(ui): add PasteConfigActivity with live decode + preview"
```

---

## Task 9: Wire sheet → activity, and register deep link

**Files:**
- Modify: `ui/src/main/java/org/amnezia/awg/fragment/TunnelListFragment.kt`
- Modify: `ui/src/main/AndroidManifest.xml`

**Interfaces:**
- Consumes: `AddTunnelsSheet.REQUEST_PASTE` (Task 6), `PasteConfigActivity` (Task 8).

- [ ] **Step 1: Handle `REQUEST_PASTE` in `TunnelListFragment`**

In the `setFragmentResultListener` `when` block (around line 117), add a branch next to `REQUEST_SCAN`:
```kotlin
                AddTunnelsSheet.REQUEST_PASTE -> {
                    startActivity(Intent(requireActivity(), PasteConfigActivity::class.java))
                }
```
Add the import at the top:
```kotlin
import org.amnezia.awg.activity.PasteConfigActivity
```

- [ ] **Step 2: Register the activity + deep link in the manifest**

In `ui/src/main/AndroidManifest.xml`, inside `<application>` (next to `TunnelCreatorActivity`), add:
```xml
        <activity
            android:name=".activity.PasteConfigActivity"
            android:exported="true"
            android:label="@string/paste_title"
            android:parentActivityName=".activity.MainActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter android:label="@string/paste_title">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="awg" />
            </intent-filter>
        </activity>
```

> `android:exported="true"` is required because the intent-filter makes it externally reachable (tap-to-import). The activity reads only `intent.data`; it never logs it.

- [ ] **Step 3: Build to verify it compiles**

Run: `./gradlew :ui:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 4: Manual end-to-end test (paste path)**

1. Produce a string: `uv run tools/awgshare/awgshare.py encode tunnel/src/test/resources/share-vector.conf`
2. Copy it to the device clipboard (e.g. `adb shell input` or paste via a chat app).
3. App → add → "Paste config string". Tap the clipboard end-icon (or confirm auto-fill).
4. Verify: green check "Valid AmneziaWG link — "awg-fi-01""; preview card shows Name `awg-fi-01`, Endpoint `192.0.2.1:51820`, Obfuscation summary; "Add tunnel" enabled.
5. Tap "Add tunnel" → returns to list with a new `awg-fi-01` tunnel.
6. Paste garbage → red "Couldn't decode…", no preview, button disabled.

- [ ] **Step 5: Manual deep-link test**

Run:
```bash
STR=$(uv run tools/awgshare/awgshare.py encode tunnel/src/test/resources/share-vector.conf | tr -d '\n')
adb shell am start -a android.intent.action.VIEW -d "$STR" <applicationId>
```
(Replace `<applicationId>` with the fork-distinct id, or omit to let the system resolve.) Expected: `PasteConfigActivity` opens with the field pre-filled and valid.

- [ ] **Step 6: Commit**

```bash
git add ui/src/main/java/org/amnezia/awg/fragment/TunnelListFragment.kt \
        ui/src/main/AndroidManifest.xml
git commit -m "feat(ui): wire paste row + awg:// deep link to PasteConfigActivity"
```

---

## Task 10: Full regression + docs touch-up

**Files:**
- Modify: `docs/superpowers/specs/2026-06-17-config-share-string-design.md` (status line)

- [ ] **Step 1: Run the whole test suite**

Run: `./gradlew :tunnel:testDebugUnitTest :ui:testDebugUnitTest`
Expected: PASS (ConfigShare + ShareStringDecoder suites green; no existing tests broken).

- [ ] **Step 2: Run the Python tests**

Run: `cd tools/awgshare && uv run --with pytest pytest -q`
Expected: PASS (4 tests).

- [ ] **Step 3: Lint/assemble release-ish to catch resource issues**

Run: `./gradlew :ui:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 4: Update the design doc status**

Change the design doc's status line from "Design only — not scheduled" to note the Android + tooling halves are implemented (server UI remains future work), referencing this plan file.

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/specs/2026-06-17-config-share-string-design.md
git commit -m "docs: mark awg://v1 Android + CLI implemented; server UI pending"
```

---

## Self-Review

**Spec coverage:**
- SPEC-add-tunnel §A (sheet: title, 4 rows, icon wells, paste row, trust warning) → Task 6. ✓
- SPEC-add-tunnel §B (paste screen: toolbar, helper, single-line mono field, clipboard end-icon, inline valid/invalid, preview card, repeated trust warning, pinned Add enabled-only-when-valid) → Tasks 7–8. ✓
- "decode live, app-side; show `paste_invalid` rather than fail silently" → Task 8 `validate()`. ✓
- Trust warning in both places, amnezia.org emphasized → Tasks 6 & 8 `emphasizeAmnezia`. ✓
- `awg://v1` format + pinned interop choices → Tasks 1–3. ✓
- Python CLI `uv run <tool> encode <conf>` → Task 2. ✓
- Deep link → Tasks 8–9. ✓
- Strings/`ic_paste` shipped → Task 5. ✓
- Accepted formats narrowed to `awg://` + raw `.conf` (copy corrected) → Global Constraints + Task 5. ✓

**Placeholder scan:** No TBD/"handle errors"/"similar to". Resource-existence checks flagged inline (`ic_arrow_back`, `ic_check`, `colorSurfaceContainerHigh`, `Interface`/`Peer` getters, ConstraintLayout-in-style attrs) are verification steps with concrete fallbacks, not placeholders.

**Type consistency:** `ConfigShare.PREFIX`/`encode`/`decode` (Task 1) used identically in Tasks 2–4. `DecodedShare(configText, config, name, endpoint, obfuscation)` (Task 4) consumed verbatim in Task 8. `REQUEST_PASTE` defined (Task 6) and consumed (Task 9). `TunnelManager.create(name, config)` matches existing usage.

## Open design note for the user (low-risk default taken)

The **`# Name = <name>` comment convention** (Global Constraints) is a small addition not in the original design doc — it's how the preview/`paste_valid` show a real tunnel name ("awg-fi-01"), since a raw `.conf` carries no name. Fallback is the endpoint host. The Python `encode` accepts a conf that already contains the comment (documented in the CLI README). When the **server UI** half is built, it should emit this comment. If you'd rather not introduce the convention, the fallback alone (endpoint host, then a naming dialog) also works — say the word and I'll drop the convention from Task 4.
